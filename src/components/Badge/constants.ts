export const BADGE_TESTIDS = {
	BASE: 'badge',
	ICON_SUFFIX: '-icon',
} as const

/** Tones of the design set, in the order the Figma grid lists them. */
export const BADGE_TONES = ['neutral', 'accent', 'amber', 'danger', 'inverse'] as const
