export const TOOLTIP_TESTIDS = {
	BASE: 'tooltip',
	CHIP_SUFFIX: '-chip',
} as const

/** Placements of the design set, in the order the Figma grid lists them. */
export const TOOLTIP_PLACEMENTS = ['top', 'right', 'bottom', 'left'] as const
